name: Run One
on:
  workflow_call:
    inputs:
      version: # the variable you can use in place of a matrix
        required: true
        type: string
jobs:
  # build-slurm:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v4
        
  #     - name: Login to GitHub Container Registry
  #       uses: docker/login-action@v3
  #       with:
  #         registry: ghcr.io
  #         username: ${{ github.repository_owner }}
  #         password: ${{ secrets.GITHUB_TOKEN }}
      
  #     - name: Set up Docker Buildx
  #       uses: docker/setup-buildx-action@v3
      
  #     - name: Build
  #       uses: docker/build-push-action@v5
  #       with:
  #         build-args: |
  #           SLURM_TAG=${{ inputs.version }}
  #         context: .
  #         push: true
  #         load: true
  #         tags: ghcr.io/${{ github.repository_owner }}/slurm:${{ inputs.version }}
  #         cache-from: type=gha
  #         cache-to: type=gha,mode=max
      
  #     - name: Inspect
  #       run: |
  #         docker image inspect ghcr.io/${{ github.repository_owner }}/slurm:${{ inputs.version }}
  job1:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'job1 version ${{ inputs.version }}'